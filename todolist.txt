# sentry.aim_new TODO

## 三相机内参：要填到哪

- [ ] `front_0` 相机内参填到 `sentry.aim_new/src/aim_camera_driver/config/gx_camera_0.yaml`
  需要改的就是这一组字段：
  `camera_fx`
  `camera_fy`
  `camera_cx`
  `camera_cy`
  `dist_k1`
  `dist_k2`
  `dist_p1`
  `dist_p2`
  `dist_k3`

- [ ] `front_1` 相机内参填到 `sentry.aim_new/src/aim_camera_driver/config/gx_camera_1.yaml`
  需要改的字段和 `front_0` 一样，也是：
  `camera_fx`
  `camera_fy`
  `camera_cx`
  `camera_cy`
  `dist_k1`
  `dist_k2`
  `dist_p1`
  `dist_p2`
  `dist_k3`

- [ ] `back` 相机内参填到 `sentry.aim_new/src/aim_camera_driver/config/usb_camera.yaml`
  需要改的字段也是：
  `camera_fx`
  `camera_fy`
  `camera_cx`
  `camera_cy`
  `dist_k1`
  `dist_k2`
  `dist_p1`
  `dist_p2`
  `dist_k3`

- [ ] 同一份内参还要同步填到 `sentry.aim_new/src/aim_solver/config/aim_solver.yaml`
  `front_0` 对应：
  `front_camera_matrix`
  `front_distortion_coefficients`

- [ ] `front_1` 对应：
  `front_1_camera_matrix`
  `front_1_distortion_coefficients`

- [ ] `back` 对应：
  `back_camera_matrix`
  `back_distortion_coefficients`

- [ ] 填写格式要注意：
  `gx_camera_0.yaml / gx_camera_1.yaml / usb_camera.yaml` 里是拆开的单个数字
  例如 `camera_fx`、`dist_k1`
  但 `aim_solver.yaml` 里要写成数组
  例如 3x3 的 `camera_matrix` 和 5 维的 `distortion_coefficients`

- [ ] 现在 `aim_solver.yaml` 里的
  `front_use_camera_info_topic`
  `front_1_use_camera_info_topic`
  `back_use_camera_info_topic`
  都还是 `false`
  所以当前代码实际用的是 `aim_solver.yaml` 里手填的内参，不是直接吃 `/camera_info`
  上车前必须确认 driver 和 solver 两边内参一致

## 三相机外参：要填到哪

- [ ] `front_0`、`front_1`、`back` 三个相机都要分别做外参标定
  外参指的是“相机相对机器人/云台坐标系的位置和姿态”
  也就是平移 `x y z` 和旋转 `yaw pitch roll`

- [ ] 现在 `sentry.aim_new/src/sentry_tf/config/sentry_tf.yaml` 里实际只有两组外参入口：
  `barrel_to_camera`
  `big_yaw_to_usb_camera`

- [ ] `back` 相机的外参现在就是填在
  `sentry.aim_new/src/sentry_tf/config/sentry_tf.yaml`
  的 `big_yaw_to_usb_camera` 下面：
  `x`
  `y`
  `z`
  `yaw`
  `pitch`
  `roll`
  `parent_frame`
  `child_frame`

- [ ] 前向工业相机的外参现在还没有拆成两路
  当前 `barrel_to_camera` 只够描述“一台前相机”
  而且 `child_frame` 现在还是 `gx_camera`，不是 `gx_camera_0` / `gx_camera_1`

- [ ] 要扩展成三相机，建议把 `barrel_to_camera` 改成两组：
  `barrel_to_gx_camera_0`
  `barrel_to_gx_camera_1`
  每组都各自填写：
  `x`
  `y`
  `z`
  `yaw`
  `pitch`
  `roll`
  `parent_frame`
  `child_frame`

- [ ] 改完 `sentry_tf.yaml` 以后，还要同步修改
  `sentry.aim_new/src/sentry_tf/launch/sentry_tf.launch.py`
  因为这个 launch 现在只会读取：
  `barrel_to_camera`
  `big_yaw_to_usb_camera`
  并发布两条静态 TF
  也就是说：只改 yaml 还不够，launch 也得一起改

## TF 树从“两相机”扩到“三相机”

- [ ] 当前 TF 现状不是“三相机完整链路”
  现在实际上是：
  `gimbal_barrel -> gx_camera`
  `gimbal_big_yaw -> usb_camera`
  其中前向只有一条静态相机 TF

- [ ] 要补成三相机，至少要能明确发布这三条链：
  `gimbal_barrel -> gx_camera_0`
  `gimbal_barrel -> gx_camera_1`
  `gimbal_big_yaw -> usb_camera`

- [ ] 同时检查各节点 `frame_id` 是否一致：
  `gx_camera_0.yaml` 里现在是 `frame_id: "gx_camera_0"`
  `gx_camera_1.yaml` 里现在是 `frame_id: "gx_camera_1"`
  `usb_camera.yaml` 里现在是 `frame_id: "usb_camera"`
  所以 TF 里的 `child_frame` 也必须和这三个名字严格一致
  不能继续写成泛化的 `gx_camera`

- [ ] 上车前建议逐条验证：
  `world/gimbal_world -> gx_camera_0`
  `world/gimbal_world -> gx_camera_1`
  `world/gimbal_world -> usb_camera`
  只要其中一条查不到，那个相机链路后面的 solver/predictor 就会不稳定

## 现在就该继续检查的点

- [ ] 检查 `sentry_tf.launch.py` 是否完成三相机静态 TF 发布
- [ ] 检查 `aim_solver.yaml` 三路 topic 和三路内参是否都已经分别写对
- [ ] 检查启动后日志是否足够清楚
  至少要能快速看出：
  哪几个节点起来了
  三路相机各自有没有出图
  detector 有没有稳定出结果
  solver / predictor / decider / controller 有没有持续输出

## 额外记录

- [ ] 现在前哨站初始化逻辑，仍然是“把第一次看到的那块板当参考高度，再按 offset 推另外两层”
  这部分我之前已经单独看过代码了
  相比旧工程，这一版确实还可以继续优化
